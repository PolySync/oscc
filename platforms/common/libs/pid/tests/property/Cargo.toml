[package]
name = "pid_pb_tests"
version = "0.0.1"
authors = ["Katie Cleary <kcleary@polysync.io>"]
build = "build.rs"

[dependencies]
quickcheck = "0.3"

[build-dependencies]
bindgen = "0.23"
gcc = "0.3"

[lib]
name = "tests"
